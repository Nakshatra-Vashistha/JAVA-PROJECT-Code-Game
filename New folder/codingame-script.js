document.addEventListener('DOMContentLoaded', () => {
    // Login button click handler
    const loginBtn = document.getElementById('login-btn');
    loginBtn.addEventListener('click', () => {
        alert('Login functionality to be implemented');
    });

    // Signup button click handler
    const signupBtn = document.getElementById('signup-btn');
    signupBtn.addEventListener('click', () => {
        alert('Signup functionality to be implemented');
    });

    // Start coding button click handler
    const startCodingBtn = document.getElementById('start-coding-btn');
    startCodingBtn.addEventListener('click', () => {
        alert('Redirecting to coding challenges');
    });

    // Explore challenges button click handler
    const exploreBtn = document.getElementById('explore-btn');
    exploreBtn.addEventListener('click', () => {
        alert('Showing available coding challenges');
    });
});
